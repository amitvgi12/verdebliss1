/**
 * Browser instrumentation placeholder.
 * Keep this file dependency-free so the production bundle and build tracer do
 * not include optional observability SDK packages. Use a log-drain backed RUM
 * provider in a separate, measured change if needed.
 */
export {}
