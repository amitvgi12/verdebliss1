{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm ci",
  "outputDirectory": ".next",
  "regions": ["bom1"],
  "build": {
    "env": {
      "VERCEL_FORCE_NO_BUILD_CACHE": "1",
      "TURBO_FORCE": "true"
    }
  },
  "_crons_note": "Scheduled tasks moved to .github/workflows/scheduled-tasks.yml — Vercel Hobby caps crons at 2 jobs with daily granularity, which is too coarse for hourly reconciliation and 15-minute ISR warming. Both routes still authenticate with Authorization: Bearer $CRON_SECRET, so re-adding a `crons` block here would work but would double-fire (harmless — both endpoints are idempotent)."
}
