export function init() {
  // Optional Sentry dependency is not installed in this build.
}

export function captureException() {
  // Observability falls back to the local structured logger.
}

export function captureMessage() {
  // Observability falls back to the local structured logger.
}
